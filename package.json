{
  "name": "feedboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run build:lib && npm run build:config",
    "build:lib": "vite build --mode library",
    "build:config": "node scripts/generate-config.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "@lit-labs/router": "^0.1.4",
    "hls.js": "^1.6.2",
    "lit": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "~5.8.0",
    "vite": "^6.2.4"
  }
}
